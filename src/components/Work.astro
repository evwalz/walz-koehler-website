---
import Todo from "./Todo.astro";
import type { SiteData } from "../lib/types";

interface Props {
  data: SiteData;
}

const { data } = Astro.props;
---

<section class="sec" id="work">
  <div class="in">
    <h2>{data.work.heading}</h2>
    <div class="works">
      {data.work.items.map((item) => (
        <article class:list={["card", "work", item.pending && "pending"]}>
          <p class="eyebrow">{item.eyebrow}</p>
          <h3><Todo value={item.title} /></h3>
          <p><Todo value={item.text} /></p>
          {item.sources.length > 0 && (
            <div class="srcs">
              {item.sources.map((source) => (
                <a class="src" href={source.href} aria-label={source.aria} target="_blank" rel="noopener">
                  {source.label} <span aria-hidden="true">↗</span>
                </a>
              ))}
            </div>
          )}
        </article>
      ))}
    </div>
  </div>
</section>
