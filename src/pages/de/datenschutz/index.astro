---
import { getEntry } from "astro:content";
import PrivacyPage from "../../../components/PrivacyPage.astro";

const entry = await getEntry("site", "de");
if (!entry) throw new Error("Missing content entry: de");
---

<PrivacyPage lang="de" data={entry.data} />
