---
import { getEntry } from "astro:content";
import LegalNoticePage from "../../../components/LegalNoticePage.astro";

const entry = await getEntry("site", "de");
if (!entry) throw new Error("Missing content entry: de");
---

<LegalNoticePage lang="de" data={entry.data} />
