---
import { getEntry } from "astro:content";
import HomePage from "../components/HomePage.astro";

const entry = await getEntry("site", "en");
if (!entry) throw new Error("Missing content entry: en");
---

<HomePage lang="en" data={entry.data} />
