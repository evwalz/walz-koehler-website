// Site-wide config. SITE_URL is the live domain; LAUNCHED is the single launch switch
// (false keeps robots.txt at "Disallow: /" and the verify-before-launch notices visible).
export const SITE_URL = "https://walz-koehler.com";
export const LAUNCHED = false;
